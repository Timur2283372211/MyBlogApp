# Используем легкий веб-сервер nginx для раздачи статичных файлов
FROM nginx:alpine

# Копируем ВСЕ ваши фронтенд-файлы в папку, которую nginx использует по умолчанию
COPY . /usr/share/nginx/html

# Nginx по умолчанию слушает порт 80
EXPOSE 80

# Команда для запуска nginx уже прописана в базовом образе